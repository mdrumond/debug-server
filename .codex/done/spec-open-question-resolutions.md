# Spec Updates: Open Question Resolutions

- Documented FastAPI as the definitive HTTP/WebSocket transport for the server API surface.
- Specified that the CLI and MCP server ship from a shared Python package for simplified distribution.
- Clarified that remote debugging initially supports `debugpy`, `gdb`, and `lldb`, ensuring expectations are scoped.
